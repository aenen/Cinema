@model IEnumerable<Cinema.DbLayer.Ticket>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderItem.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TicketPrice.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderItemId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TicketPriceId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsUsed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrderItem.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TicketPrice.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderItemId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TicketPriceId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsUsed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
